version: '3.8'

services:
  paper-trading:
    build: .
    container_name: indian_equity_paper_engine
    restart: unless-stopped
    ports:
      - "8080:8080"
    volumes:
      - ./database:/app/database
      - ./reports:/app/reports
      - ./logs:/app/logs
    env_file:
      - .env
